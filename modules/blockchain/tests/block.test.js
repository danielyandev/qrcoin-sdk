// const {Block} = require('../build/index')
//
// test('Create block', () => {
//     const block = new Block(Date.now(), 'last-hash', 'new-hash', [], 'test-validator', 'test-signature')
//     expect(block.hash === Block.blockHash(block))
// })

// TODO fix test
test('test placeholder', () => {
    expect(true).toBeTruthy()
})